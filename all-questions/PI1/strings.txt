Sta vraca poziv funkcije strlen u sledecem kodu:
	#include <stdio.h>
	#include <string.h>
	int main()
	{
		char a[]={'1','2','3','\0','4','\0'};
		printf("%d\n", strlen(a));

		return 0;
	}
-5
-4
-6
+3
.

Sta nam vraca poziv funkcije strlen u sledecem kodu:
	#include <stdio.h>
	#include <string.h>
	int main()
	{
		char a[]={'1','2','3','0','4','\0'};
		printf("%d\n", strlen(a));

		return 0;
	}
+5
-4
-6
-3
.

Sta vraca poziv funkcije strlen u sledecem kodu:
	#include <stdio.h>
	#include <string.h>
	int main()
	{
		char a[]={'0','1','2','3','\0','4','0'};
		printf("%d\n", strlen(a));

		return 0;
	}
-5
+4
-6
-3
.

Sta vraca poziv funkcije strlen u sledecem kodu:
	#include <stdio.h>
	#include <string.h>
	int main()
	{
		char a[]={'\0','1','2','3','0','4','\0'};
		printf("%d\n", strlen(a));

		return 0;
	}
-5
-4
-6
-3
+0
.

Data je rec "0123456789", najmanji string u koji se ona moze upisati u celosti je:
+char str[11];
-char str[10];
-char str[9];
.

Data je rec "012346789", najmanji string u koji se ona moze upisati u celosti je:
-char str[11];
+char str[10];
-char str[9];
.

Data je rec "123456789", najmanji string u koji se ona moze upisati u celosti je:
-char str[11];
+char str[10];
-char str[9];
.

Data je rec "12346789", najmanji string u koji se ona moze upisati u celosti je:
-char str[11];
-char str[10];
+char str[9];
.

Najmanji string u koji se moze u celosti uspesno upisati podatak "" je:
-char str[0];
+char str[1];
-char str[2];
.

Da bi prikazali na ekranu string char str[]="Primer prvog stringa"; u celosti to mozemo uraditi sa:
+Oba ponudjena
-iskljucivo: printf("\n%s",str);
-iskljucivo: puts(str);
.

Da bi procitali sa tastature string char str[]="Primer prvog stringa"; u celosti to mozemo uraditi sa:
-Oba ponudjena
-iskljucivo: scanf("%s",str);
+iskljucivo: gets(str);
.

Da bi procitali sa tastature string char str[]="Primer_prvog_stringa"; u celosti to mozemo uraditi sa:
+Oba ponudjena
-iskljucivo: scanf("%s",str);
-iskljucivo: gets(str);
.

Da bi prikazali na ekranu string char str[]="Primer prvog stringa"; u celosti to mozemo uraditi sa:
-Oba ponudjena
-gets(str);
+puts(str);
.

Da bi procitali sa tastature string char str[]="Primer prvog stringa"; u celosti to mozemo uraditi sa:
-Oba ponudjena
+gets(str);
-puts(str);
.

Nakon poziva funkcije 'strcmp(str1,str2)' dobili smo povratnu vrednost 0, sto nam govori da:
+su str1 i str2 istog sadrzaja
-su str1 i str2 razliciti
-je str1 manji od str2
-je str2 manji od str1
-smo uspesno kopirali vrednost stringa str2 u str1
-smo uspesno kopirali vrednost stringa str1 u str2
.
