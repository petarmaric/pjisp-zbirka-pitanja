Parametri funkcije su:
+lokalne promenljive
-globalne promenljive
-nisu promenljive
-neinicijalizovane lokalne promenljive
-neinicijalizovane globalne promenljive
.

Oblast vazenja parametara funkcija je:
-oblast vazenja parametara nije ogranicena
-zaglavlje funkcije
-ceo fajl
+funkcija
.

Koji ce biti rezultat izvrsavanja sledeceg koda:
	void f(int a) {
		a = 3;
	}
	int main() {
		int a = 5;
		f(a);
		printf("%i", a);
	}
+5
-3
-Prilikom kompajliranja dobijamo upozorenje da u pokazivac upisujemo ceo broj.
.

Koji ce biti rezultat izvrsavanja sledeceg koda:
	void f(int *i) {
	   *i = 3;
	}
	int main() {
	   int a = 5;
	   f(&a);
	   printf("%d", a);
	   return 0;
	}
-5
+3
-Prilikom kompajliranja dobijamo upozorenje da u pokazivac upisujemo ceo broj.
.

Koji ce biti rezultat izvrsavanja sledeceg koda:
	void f(int *i) {
	   i = 3;
	}
	int main() {
	   int a = 5;
	   f(&a);
	   printf("%i", a);
	   return 0;
	}
-5
-3
+Prilikom kompajliranja dobijamo upozorenje da u pokazivac upisujemo ceo broj.
.

Nakon izvršavanja sledećeg bloka koda izlaz će biti:
	#include <stdio.h>
	void f(int *i) {
		int a = 1;
		*i = 5;
	}
	int main() {
		int a = 2;
		int i = 3;
		printf("a = %d", a);
		f(&a);
		return 0;
	}
- a = 1
+ a = 2
- a = 5
- a = 3
.

Nakon izvršavanja sledećeg bloka koda izlaz će biti:
	#include <stdio.h>
	void f(int *i) {
		int a = 1;
		*i = 5;
	}
	int main() {
		int a = 2;
		int i = a;
		f(&a);
		printf("a = %d", i);
		return 0;
	}
- a = 1
+ a = 2
- a = 5
- a = 3
.

Nakon izvršavanja sledećeg bloka koda izlaz će biti:
	#include <stdio.h>
	int f(int *i) {
		int b = 1;
		*i = 5;
		return b;
	}
	int main() {
		int a = 2;
		int i = 3;
		a = f(&a);
		printf("a = %d", a);
		return 0;
	}
+ a = 1
- a = 2
- a = 5
- a = 3
.

Nakon izvršavanja sledećeg bloka koda izlaz će biti:
	#include <stdio.h>
	int f(int *i) {
		int a = 1;
		*i = 5;
		return a;
	}
	int main() {
		int a = 2;
		int i = 3;
		printf("a = %d", a);
		a = f(&a);
		return 0;
	}
- a = 1
+ a = 2
- a = 5
- a = 3
.

Koja ce vrednost biti odstampana nakon izvrsavanja sledeceg koda:
	#include <stdio.h>
	int f(int *i, int a)
	{
		a=*i;
		*i = 5;
		return a;
	}
	int main()
	{
		int a=2;
		int i=3;
		a=f(&i,a);
		printf("%d",a);
		return 0;
	}
-5
+3
-1
-2
.

Koja ce vrednost biti odstampana nakon izvrsavanja sledeceg koda:
	#include <stdio.h>
	int f(int *i, int a)
	{
		a=*i;
		*i = 5;
		return a;
	}
	int main()
	{
		int a=2;
		int i=3;
		a=f(&i,a);
		printf("%d",i);
		return 0;
	}
+5
-3
-1
-2
.

Kako treba da izgleda deklaracija funkcije "unesiNiz", koja nema povratnu vrednost i poziva se iz sledeceg dela koda:
	int main()
	{
		unsigned NizTacaka[1000], n;
		unesiNiz(NizTacaka,&n);
		ispisi(NizTacaka,n);
		return 0;
	}
+ void unesiNiz(unsigned *, unsigned *);
- unsigned unesiNiz(unsigned *, unsigned *);
- void unesiNiz(unsigned *, unsigned);
- void unesiNiz(unsigned, unsigned *);
- void unesiNiz(unsigned, &unsigned);
.

Kog je tipa povratna vrednost funkcije:
	void zbir(int x, int y, int *k) {
		*k=x+y;
	}
-int
-int *
-float
+Ova funkcija nema povratnu vrednost
.

Kog je tipa povratna vrednost funkcije:
	int *zbir(int x, int y, int *k) {
		*k=x+y;
	}
-int
+int *
-float
-Ova funkcija nema povratnu vrednost
.
